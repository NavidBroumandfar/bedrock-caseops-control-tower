"""
app/evaluation — F-2 local evaluation harness.

Modules:
  loader  — load EvaluationCase and ExpectedOutput from F-1 fixtures
  scorer  — score a candidate CaseOutput against an ExpectedOutput
  runner  — orchestrate a batch evaluation run and produce typed results
"""
